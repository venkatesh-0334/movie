<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-image: url('https://cdn.pixabay.com/photo/2022/09/21/17/02/blue-background-7470781_960_720.jpg'); 
      background-size: cover;
      background-color: #141414; 
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 20px;
    }

    h1 {
      text-align: center;
      color: #fff; 
      margin-bottom: 20px;
      font-size: 36px;
      font-weight: bold;
    }

    form {
      max-width: 400px;
      padding: 20px;
      background-color: #333; 
      border: 1px solid #444; 
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
      border-radius: 10px;
    }

    input[type="text"] {
      width: 100%;
      height: 40px;
      margin-bottom: 20px;
      padding: 10px;
      border: 1px solid #555;
      border-radius: 5px;
      background-color: #333; 
      color: #fff; 
    }

    input[type="text"]:focus {
      border-color: #666;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.2);
    }

    button[type="submit"] {
      width: 100%;
      height: 40px;
      background-color: #e50914; 
      color: #fff; 
      padding: 10px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button[type="submit"]:hover {
      background-color: #c40303; 
    }

    ul {
      list-style: none;
      padding: 0;
      margin: 0;
    }

    li {
      padding: 10px;
      border-bottom: 1px solid #444; 
    }

    li:hover {
      background-color: #333; 
    }

    li:last-child {
      border-bottom: none;
    }
  </style>
</head>
<body>
  <h1>Dashboard</h1>
  <form method="get" action="/dashboard">
    <input type="text" name="search" placeholder="Search for a movie...">
    <button type="submit">Search</button>
  </form>
  <ul>
    <% movies.forEach(movie => { %>
      <li><%= movie.Title %> (<%= movie.Year %>)</li>
    <% }) %>
  </ul>
  </div>
</body>
</html>